# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Keep the code graph updated

This repo maintains a dependency graph in `graphify-out/`. After EVERY iteration (any code change or edit), regenerate it so the context stays current:

```bash
graphify update .
```
